spring.application.name=TravelWise-backend

#PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/travel_wise_demo
spring.datasource.username=postgres
spring.datasource.password=1234
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database=postgresql
spring.jpa.hibernate.ddl-auto=none

#Cloudinary Credentials
cloudinary.cloud_name=dwcxmqpog
cloudinary.upload_preset=dev_unsigned

# AWS Credentials
cloud.aws.credentials.access-key="dummy"
cloud.aws.credentials.secret-key="dummy"

# AWS Region
cloud.aws.region.static=eu-central-1

# AWS Stack
cloud.aws.stack.auto=false

# Application bucket name
application.bucket.name=trip-wise-storage